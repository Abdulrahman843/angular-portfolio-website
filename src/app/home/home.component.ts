import { Component } from '@angular/core';

@Component({
  selector: 'app-home', // The HTML tag used to embed this component
  standalone: true,
  imports: [], // Used for standalone component imports if needed
  templateUrl: './home.component.html', // Template file for the component's HTML
  styleUrls: ['./home.component.css'] // Stylesheet(s) for the component
})
export class HomeComponent {
  // Add properties or methods here as needed for the home page
}
